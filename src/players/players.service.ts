import { Player } from "src/domain/Player";

export class PlayersService {
  constructor() {}

  getAll(): Player[] {
    return [];
  }

  getSingle(id: string): Player | undefined {
    return undefined;
  }
}
